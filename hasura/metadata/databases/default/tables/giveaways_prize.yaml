table:
  name: prize
  schema: giveaways
select_permissions:
- permission:
    columns:
    - id
    - created_at
    - updated_at
    - name
    - description
    - giveaway
    - owner
    filter:
      owner:
        _eq: X-Hasura-User-Id
  role: user
