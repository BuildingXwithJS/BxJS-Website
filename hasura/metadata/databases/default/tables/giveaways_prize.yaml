table:
  schema: giveaways
  name: prize
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - created_at
        - updated_at
        - name
        - description
        - giveaway
        - owner
      filter:
        owner:
          _eq: X-Hasura-User-Id
